// 不对
var addBinary = function (a, b) {
//   const maxL = Math.max(a.length, b.length)
//   a = a.padStart(maxL, '0')
//   b = b.padStart(maxL, '0')
//   let result = []
//   let flag = 0
//   for (let i = maxLen - 1; i >= 0; i--) {
//     const ta = a[i] || 0; const tb = b[i] || 0
//     result[i] = ta + tb + (flag ? 1 : 0)
//     flag = result[i] >= 2
//     result[i] %= 2
//   }
//   result = (flag ? '1' : '') + result.join('')
//   return result
}
console.log(addBinary(11, 1))
